namespace DrumDuino.App.ViewModels;

public enum AppPage
{
    Configuration,
    Analytics,
    Training
}

public enum EditorSection
{
    Identity,
    Sensitivity,
    Curve,
    Crosstalk
}

public enum ConnectionMode
{
    Disconnected,
    Tool,
    Midi
}
