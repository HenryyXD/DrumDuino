namespace DrumDuino.App.ViewModels;

public enum AppPage
{
    Configuration,
    Analytics
}

public enum EditorSection
{
    Identity,
    Sensitivity,
    Curve,
    Crosstalk
}

public enum ConnectionMode
{
    Disconnected,
    Tool,
    Midi
}
