# DrumDuino Config App

PC configuration tool for the DrumDuino firmware.

**Status:** Not implemented yet.

Planned features:

- Connect via COM port (115200)
- Edit 16 pads with live preview
- Import legacy `pins.ini` presets
- Apply all settings + save EEPROM
- Return to MIDI mode on exit
